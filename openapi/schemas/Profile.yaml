type: object
required:
  - id
  - full_name
properties:
  id:
    type: string
    format: uuid
  full_name:
    type: string
  avatar_url:
    type: string
    format: uri
    nullable: true
    description: Public/signed URL resolved from the stored avatar_path, not the raw path itself.
